#include <stdio.h>
/**
 * main - Main function
 * Description: program that checks lowercase character
 * Return: Prints the numbers
 */
int main(void)
{
	int _islower(int c);
	{
	return	(c >= 'a' && c <= 'z');
	}
return (0);
}
