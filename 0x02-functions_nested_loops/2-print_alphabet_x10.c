#include <stdio.h>
/**
 * main - Main function
 * Description:	program that prints 10 times the alphabet in lowercase
 * Return: prints the numbers
 */
int main(void)
{
	char(ch);
	int i;

	i = 0;

	while	(i > 10)
	{
	ch = 'a';
	while (ch <= 'z')
	{
		_putchar(ch);
		ch++;
	}
	_putchar('\n');
	i++
return (0);
}
