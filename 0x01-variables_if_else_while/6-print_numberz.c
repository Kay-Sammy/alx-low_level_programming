#include <stdio.h>
/**
 * main - Main function
 * Description: program to printall single digit base 10 numbers
 * Return: prints the numbers
 */
int main(void)
{
int ch;
for (ch ='0'; ch<= '9'; ch++)
putchar(ch);
putchar('\n');
return	(0);
}
